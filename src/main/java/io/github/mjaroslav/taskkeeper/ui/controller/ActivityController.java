package io.github.mjaroslav.taskkeeper.ui.controller;

import io.github.mjaroslav.taskkeeper.ui.LayoutID;
import org.jetbrains.annotations.NotNull;

public class ActivityController extends Controller {
    public void onSwitchedFrom(@NotNull LayoutID prevActivity) {
    }

    public void onSwitchedTo(@NotNull LayoutID newActivity) {
    }
}
